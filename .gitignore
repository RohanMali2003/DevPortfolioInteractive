# Ignore node_modules (prevents pushing installed packages)
node_modules/

# Ignore dependency lock files (optional, but recommended)
package-lock.json
yarn.lock

# Ignore logs
npm-debug.log
yarn-debug.log
yarn-error.log

# Ignore environment variables (if you ever use a .env file)
.env

# Ignore build files (if you later generate them)
dist/